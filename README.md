# 2026 法西瑞之旅 - 交互式行程规划

这是一个基于 Next.js + Tailwind CSS 构建的交互式旅行规划应用，用于 2026 年 5 月 31 日至 6 月 15 日的法国、西班牙（马略卡）、瑞士家庭旅行。

## ✨ 功能特性

### 📅 行程安排
- **城市筛选**：支持查看全部行程，或仅查看巴黎、马略卡、瑞士的行程
- **巴黎模式切换**：一键在「法网观赛版本」和「非法网版本」之间切换
- **详细信息**：每日行程包含日期、城市、标签、住宿信息、活动安排

### 💰 预算追踪
- **自动分类汇总**：按法国（巴黎）、西班牙（马略卡）、瑞士、全程总计自动分类
- **多币种支持**：支持 EUR、CHF、SGD 三种货币，自动换算为新币
- **分类详情**：住宿、交通、餐饮、门票、活动等细分类别
- **实时汇总**：自动计算各地区小计和全程总预算

### 🎨 特色设计
- 🎨 **深色主题**：优雅的深色配色方案，减少眼睛疲劳
- 📱 **响应式设计**：完美适配桌面端和移动端
- ⚡ **快速切换**：无刷新的标签页和模式切换
- 💎 **玻璃态设计**：现代化的毛玻璃效果
- 🎯 **清晰分类**：不同地区用不同颜色标识（巴黎-红、马略卡-蓝、瑞士-绿）

## 🛠️ 技术栈

- **框架**：Next.js 14 (App Router)
- **语言**：TypeScript
- **样式**：Tailwind CSS
- **部署**：Vercel

## 📁 项目结构

```
travel_planning_2026_FSS/
│
├── 📄 README.md                    # 项目文档（本文件）
├── 📄 .gitignore                   # Git 忽略规则
├── 📄 2026_tennis_travel.html      # 原始 HTML 行程表
├── 📄 2026 法西瑞之旅.pdf          # 旅行规划 PDF
│
└── 📁 travel-app/                  # Next.js 应用
    ├── app/                        # 页面和组件
    │   ├── components/
    │   │   ├── BudgetView.tsx      # 预算追踪视图
    │   │   ├── DayCard.tsx         # 单日行程卡片
    │   │   └── ItineraryView.tsx   # 行程视图
    │   ├── globals.css             # 全局样式
    │   ├── layout.tsx              # 根布局
    │   └── page.tsx                # 主页面（Tab 切换）
    │
    ├── data/                       # 数据文件
    │   ├── budget.ts               # 预算数据
    │   ├── itinerary.ts            # 16 天行程数据
    │   └── types.ts                # TypeScript 类型定义
    │
    ├── package.json                # 项目依赖
    ├── next.config.js              # Next.js 配置
    ├── tailwind.config.ts          # Tailwind 配置
    ├── tsconfig.json               # TypeScript 配置
    └── vercel.json                 # Vercel 部署配置
```

## 🚀 快速开始

### 安装依赖

```bash
cd travel-app
npm install
```

### 开发模式

```bash
npm run dev
```

访问 [http://localhost:3000](http://localhost:3000) 查看应用。

### 构建生产版本

```bash
npm run build
npm start
```

## 🌐 部署到 Vercel

### 方法 1：通过 Vercel CLI（推荐 - 最快速）

#### Step 1: 安装 Vercel CLI

```bash
npm install -g vercel
```

#### Step 2: 导航到应用目录

```bash
cd travel-app
```

#### Step 3: 登录 Vercel

```bash
vercel login
```

这将打开浏览器进行身份验证。

#### Step 4: 部署应用

```bash
vercel
```

**首次部署时的提示：**
- `Set up and deploy?` → 选择 **Yes**
- `Which scope?` → 选择你的账户
- `Link to existing project?` → 选择 **No**
- `What's your project's name?` → 输入 `2026-tennis-travel`（或按 Enter 使用默认名称）
- `In which directory is your code located?` → 按 **Enter**（使用当前目录）
- `Want to override the settings?` → 选择 **No**

**部署成功后会得到预览 URL：** `https://2026-tennis-travel-xxx.vercel.app`

#### Step 5: 部署到生产环境（可选）

```bash
vercel --prod
```

生产环境 URL：`https://2026-tennis-travel.vercel.app`

---

### 方法 2：通过 Git 集成（自动部署）

#### Step 1: 提交代码到 GitHub

```bash
# 从项目根目录执行
git add .gitignore README.md "2026 法西瑞之旅.pdf" 2026_tennis_travel.html travel-app/

# 提交
git commit -m "Add interactive travel planning app with Next.js

- Created React app with itinerary and budget views
- Added Paris RG/non-RG toggle feature
- Implemented automatic budget categorization by country
- Ready for Vercel deployment"

# 推送到 GitHub
git push
```

#### Step 2: 在 Vercel 导入项目

1. 访问 [https://vercel.com](https://vercel.com) 并登录
2. 点击 **"Add New Project"**
3. 选择 **"Import Git Repository"**
4. 选择你的 GitHub 仓库 `travel_planning_2026_FSS`
5. **重要配置：**
   - **Root Directory**: 设置为 `travel-app` ⚠️
   - **Framework Preset**: Next.js（自动检测）
   - **Build Command**: `npm run build`（自动填充）
   - **Output Directory**: `.next`（自动填充）
6. 点击 **"Deploy"**

#### Step 3: 自动部署设置

部署成功后，每次 `git push` 都会自动触发新的部署。

---

### ⚠️ 重要提示

由于 Next.js 应用在 `travel-app/` 子目录中，请确保：

- **Vercel CLI 方法**：在 `travel-app/` 目录内运行 `vercel` 命令
- **Git 集成方法**：在 Vercel 项目设置中将 Root Directory 设置为 `travel-app`

---

### 📱 部署后

你的应用将在以下地址可访问：
- **预览环境**: `https://your-project-git-branch.vercel.app`
- **生产环境**: `https://your-project.vercel.app`

**功能特性：**
- ✅ 全球 CDN 加速访问
- ✅ 自动 HTTPS
- ✅ 随时随地查看行程和预算
- ✅ 分享链接给家人朋友
- ✅ Git push 自动部署（使用 Git 集成）
- ✅ 可添加自定义域名

**常用命令：**
```bash
vercel ls        # 查看部署列表
vercel inspect   # 查看项目详情
```

## 🔧 自定义数据

### 修改行程

编辑 `travel-app/data/itinerary.ts` 文件，更新 `days` 数组中的行程信息。

### 修改预算

编辑 `travel-app/data/budget.ts` 文件，更新 `budgetItems` 数组中的预算项目。

### 调整汇率

在 `travel-app/app/components/BudgetView.tsx` 中更新 `EXCHANGE_RATES` 常量：

```typescript
const EXCHANGE_RATES = {
  EUR: 1.44,  // 根据实际汇率调整
  CHF: 1.52,  // 根据实际汇率调整
  SGD: 1.0,
};
```

## 📄 其他文件说明

- **2026_tennis_travel.html** - 原始静态 HTML 行程表（可独立在浏览器中打开使用）
- **2026 法西瑞之旅.pdf** - 详细旅行规划文档

## 📝 许可

本项目为个人旅行规划工具，仅供参考使用。
